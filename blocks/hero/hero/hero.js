export default async function decorate(block) {
  // The hero block has one row with two cells:
  // Cell 1: image (background/foreground)
  // Cell 2: text content (h1, p, CTA)
  // No additional restructuring needed — CSS handles the layout.
}
